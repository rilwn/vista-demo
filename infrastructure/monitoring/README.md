# Monitoring

The API exposes separate liveness and dependency readiness endpoints. The final
metrics, log aggregation, notification routes, retention, and alert thresholds
depend on the deployment and operations decision `DEP-001`.
