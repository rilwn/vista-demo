# Local rehearsal only. Generate each secret separately with openssl rand -hex 32.
# Keep this file private after copying it to .env. Never reuse production secrets.
VISTA_IMAGE_TAG=local-0.1.0
POSTGRES_PASSWORD=
S3_SECRET_ACCESS_KEY=
SESSION_SECRET=
TOTP_ENCRYPTION_KEY=
OPERATIONS_PORT=5473
POS_PORT=5474
RECOVERY_PORT=5475
